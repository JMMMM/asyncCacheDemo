subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'

    version = '0.1'

    sourceCompatibility = 1.8
    targetCompattibility = 1.8

    [compileJava, compileTestJava, javadoc]*.options*.encoding = 'UTF-8'

    ext {

    }

    repositories {
        mavenCentral()
    }

    dependencies {
        //通用依赖
        compile()
    }
}
