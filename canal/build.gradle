buildscript {
    repositories {
        mavenLocal()
    }

//    dependencies {}
}

archivesBaseName = 'canal'

//ext {}

dependencies {
    compile group: 'com.alibaba.otter', name: 'canal.parse', version: '1.1.0'
}